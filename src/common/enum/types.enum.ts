export enum UserType {
  lessor = 'lessor',
  tenant = 'tenant',
}

export enum OrganisationType {
  flat = 'flat',
  hotel = 'hotel',
  commoncourse = 'commoncourse',
  inn = 'inn',
  concession = 'concession',
}
