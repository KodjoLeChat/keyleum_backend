export enum FlatState {
  enabled = 'enabled',
  disabled = 'disabled',
  deleted = 'deleted',
  archived = 'archived',
  rented = 'rented',
}

export enum FlatImagesState {
  enabled = 'enabled',
  disabled = 'disabled',
  deleted = 'deleted',
  archived = 'archived',
  rented = 'rented',
}

export enum UserState {
  enabled = 'enabled',
  deleted = 'deleted',
}
